/* ═══════════════════════════════════════════════════════════
   ХЕЛПЕР ДЛЯ ЗАДАНИЙ
   Q('предложение с ___', ['+верный', 'почему верно'], ['неверный', 'почему нет'], ...)
   Плюс в начале варианта помечает правильный ответ.
   ═══════════════════════════════════════════════════════════ */
const Q = (s, ...opts) => ({
  s,
  o: opts.map(([t, w]) => ({ t: t[0] === '+' ? t.slice(1) : t, ok: t[0] === '+', w }))
});
